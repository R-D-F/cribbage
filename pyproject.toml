[project]
name = "cribbage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "riley", email = "rifrancis22@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
cribbage = "cribbage:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
